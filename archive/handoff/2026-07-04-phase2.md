# 引継ぎメモ（Handoff）

CLAUDE.mdの課題2「新しいチャットへ移る際、前チャットの内容を要約して引き継ぐと、情報が欠けたり表現が変わったりして意図しない方向へ進む」への対策。

**方針：自然文で要約しない。構造化された定型フォーマットで機械的に埋める。** 要約者（自分）の解釈が入る余地を減らすことが目的。

## 使い方

1. フェーズ（または長くなったセッション）を終える時、下の「最新の引継ぎ」ブロックを**今回の内容で上書き**する
2. 上書き前の内容は `archive/handoff/` に日付付きファイルとしてコピーしてから上書きする（履歴を残す）
3. 新しいチャットの最初にこのファイルの「最新の引継ぎ」だけを読めば再開できる状態を保つ

## テンプレート

```
### 完了したフェーズ
- Phase X: <フェーズ名>

### 確定した決定事項（変更しない前提）
- <決定1>（根拠：<理由>、記録先：<ファイルパス>）
- <決定2>

### 保留・要確認事項（次セッションで先に聞くこと）
- <question 1>

### 次セッションが最初に読むべきファイル
- <path1>
- <path2>

### 次にやる具体アクション
1. <action>
2. <action>
```

---

## 最新の引継ぎ

### 完了したフェーズ
- Phase 2: Workflow設計（旧引継ぎは `archive/handoff/2026-07-04-phase1.md`）

### 確定した決定事項（変更しない前提）
- Workflowの制御モデルは「直列メタ骨格（理解→計画→作業→評価→記録）＋限定ゲート分岐」。分岐はゲート3点のみ、遷移先は「進む／前工程へ戻る／計画へ戻る（中止）」の3種に固定（根拠：迷子防止を表現力より優先、記録先：`docs/decisions/0007-workflow-model.md`、`outputs/workflows/02-orchestration-principles.md` 原則W1）
- 人の承認は3ゲート制のみ：G1計画承認・G2評価裁定・G3完了確認。線引き基準は「人が判断しないと誤りの修正コストが不可逆に増えるか」（記録先：同ADR、原則W2）
- Phase 1保留の「入力欠落時の停止規約」は、Workflowの各ステップ開始条件（Skill前提入力の【必須】項目の充足確認。欠落時は停止して要求、推測補完禁止）として解消した（記録先：原則W3）
- ゲート＝チェックポイント（F2必須）＝セッション分割点。「1セッション1ゲート区間」を推奨上限とし、再開は必ずF3経由（記録先：原則W4）
- Workflowはテンプレート4本（W1新機能追加／W2バグ修正／W3リファクタリング／W4調査のみ）＋メタ骨格からの組み立て規則のハイブリッド。フェーズのスキップ判定は「成果物ファイルの存在」で行う。テンプレート追加の既定は「追加しない」（記録先：`outputs/workflows/01-workflow-catalog.md` 8・9章、原則W5）
- プロジェクト固有値（観点リスト・テストコマンド等）はWorkflowにも埋め込まない。注入原則（Phase 1原則1）のWorkflowへの拡張。WorkflowはSkillをIDで参照する一方向依存（記録先：原則W6）

### 保留・要確認事項（次セッションで先に聞くこと）
- 原則W3の停止規約を実行時にどう強制するか、および注入されるべき固有知識（レビュー観点リスト・命名規則・引継ぎテンプレート等）の恒久的な置き場所・構造は未設計。**Phase 3（Context/RAG設計）の中心要件として扱うこと**
- ゲート形骸化の検出・「同じ組み立て3回でテンプレート化」の計測・テンプレート見直し周期は未定義。Phase 7（運用設計）に委ねる
- Workflowを実行可能な形（Claude Code形式のコマンド等）で実装するかは未決定。現時点は設計ドキュメントのみ（ADR-0003の原則。Skillsで行ったADR-0006同様の部分改定を行うなら別途ADRが要る）

### 次セッションが最初に読むべきファイル
- `docs/current-state.md`
- `requests/003-rag.md`
- `outputs/skills/02-design-principles.md`（特に「Skill/Workflow/Contextの責務境界」節。Phase 3の入力）
- `outputs/workflows/02-orchestration-principles.md`（特に原則W3・W6。Phase 3へ持ち越した要件の出どころ）

### 次にやる具体アクション
1. `requests/003-rag.md` の「検討すべき問い」を確認し、Context/RAG設計を開始する
2. Phase 1・2から持ち越した要件（固有知識の置き場所、前提入力の注入の仕組み、停止規約の実行時強制）をContext設計の要件に組み込む
3. 成果物ができ次第 `outputs/context-rag/` に保存し、`docs/current-state.md` と本ファイルを更新する
