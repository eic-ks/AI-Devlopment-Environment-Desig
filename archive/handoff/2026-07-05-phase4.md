# 引継ぎメモ（Handoff）

CLAUDE.mdの課題2「新しいチャットへ移る際、前チャットの内容を要約して引き継ぐと、情報が欠けたり表現が変わったりして意図しない方向へ進む」への対策。

**方針：自然文で要約しない。構造化された定型フォーマットで機械的に埋める。** 要約者（自分）の解釈が入る余地を減らすことが目的。

## 使い方

1. フェーズ（または長くなったセッション）を終える時、下の「最新の引継ぎ」ブロックを**今回の内容で上書き**する
2. 上書き前の内容は `archive/handoff/` に日付付きファイルとしてコピーしてから上書きする（履歴を残す）
3. 新しいチャットの最初にこのファイルの「最新の引継ぎ」だけを読めば再開できる状態を保つ

## テンプレート

```
### 完了したフェーズ
- Phase X: <フェーズ名>

### 確定した決定事項（変更しない前提）
- <決定1>（根拠：<理由>、記録先：<ファイルパス>）
- <決定2>

### 保留・要確認事項（次セッションで先に聞くこと）
- <question 1>

### 次セッションが最初に読むべきファイル
- <path1>
- <path2>

### 次にやる具体アクション
1. <action>
2. <action>
```

---

## 最新の引継ぎ

### 完了したフェーズ
- Phase 4: Knowledge Management設計（旧引継ぎは `archive/handoff/2026-07-05-phase3.md`）

### 確定した決定事項（変更しない前提）
- KB（L4）の分類は**「知識の種類」5分類（知見・決定・外部情報・検索失敗・棚卸し）で固定**。分類は書き込みイベント（E3/E2等）と1対1対応し、書き込み時の裁量ゼロ。分類の追加・変更にはADRを要する。トピック（技術領域）はディレクトリにせずファイル名とタグで持つ（根拠：分類がイベントから一意に決まる時だけ検索の第1歩が機械的になる、記録先：`docs/decisions/0008-kb-structure.md`、`outputs/knowledge-management/01-kb-structure.md` 1章）
- 記録は**1エントリ1ファイル＋定型ヘッダ**（更新日・分類・タグ・情報源＋取得日・ステータス〔有効/失効＋置換先〕）。命名は `{日付}-{内容を表す短い説明}.md` で**ファイル名自体を索引にする**。これがE3の【必須】入力 `context/kb-format.md` の実体になる（記録先：同01の2〜3章）
- **L4にインデックスファイル（`MEMORY.md`的な一覧）は持たない**。写しは古びるため。一覧が必要なら `ls` から使い捨て生成する（正はファイルシステム）（記録先：ADR-0008、同01の4章）
- 重複排除・整合性確認は**書き込み時（保存前にglob＋grep、ヒットなら新規作成せず既存更新）と参照時（矛盾の即時対処）の分散実行**。定期棚卸しでのL4全量チェックは**しない**（棚卸しは検索失敗件数と更新日異常のシグナル集計のみ）。原点課題「毎回依頼する」はSkill手順への固定で解消（記録先：`outputs/knowledge-management/02-knowledge-lifecycle.md` 2・5章）
- **バージョン管理はGitのみ**。`archive/` 方式（上書き前コピー）はL4に使わない（死骸が検索を汚す）。陳腐化は失効マーキング（ステータス＋置換先）または削除（Gitに残るため安全側）（記録先：ADR-0008、同02の4章）
- 外部情報の取り込みは**情報源URL・取得日を必須メタデータにした可逆な要点抽出**の定型。Phase 3の要約禁止（圧縮の文脈）の緩和ではなく適用範囲の明確化（記録先：同02の3〜4章）
- **Phase 3との矛盾ゼロ・二重定義ゼロを検証済み**。Phase 3側ADRの見直しは不要。境界線は「器と枠組み＝Phase 3／中身と手順＝Phase 4」（記録先：`outputs/knowledge-management/03-phase3-boundary.md`）
- **Phase 2〜4の設計を実行可能形式に変換した**（ADR-0003の部分改定②）：W1〜W4＋組み立て規則＝`workflows/` の5スキル、Context/KB物理構造の生成＝`scaffold/context-scaffold`（AGENT.md雛形・INDEX雛形・kb-format.md同梱、冪等）。判断基準・原則系文書は設計のまま。設計が正・変換時の判断は各READMEの「意図的な差分」に記録。`~/.claude/skills/` へsymlink設置済み（根拠：Phase 1と同じ配布経路、記録先：`docs/decisions/0009-build-executable-workflows-and-scaffold.md`）

### 保留・要確認事項（次セッションで先に聞くこと）
- **適用作業は未実施**：①このリポジトリのCLAUDE.md軽量化（`outputs/context-rag/02-placement-criteria.md` 5章で判定済み）②`context/kb-format.md` の実体化（内容は01-kb-structure.md）③`research/` の移行（01の6章の3点：分類軸の取り替え・ヘッダ拡充・分類固定制）。いずれも設計フェーズ完了後の適用作業。実施タイミングをユーザーに確認すること
- 命名の質の劣化検出（検索失敗集計の詳細）・死蔵エントリの誤情報の扱い・昇格①の回数集計・降格の参照検出 → **Phase 7（運用設計）**に委ねる
- Phase 2〜4の実行可能形式は実装済み（ADR-0009）だが、**このリポジトリ自体は未コミット**（ユーザー判断でコミットを保留中。Phase 3/4完了処理＋ADR-0009＋workflows/＋scaffold/ が作業ツリーにある）

### 次セッションが最初に読むべきファイル
- `docs/current-state.md`
- `requests/005-instruction-methods.md`
- `outputs/skills/01-skill-catalog.md`（Phase 5の依存：Skills一覧。「呼び出す指示」の対象）
- `outputs/workflows/01-workflow-catalog.md`・`02-orchestration-principles.md`（Phase 5の依存：Workflow一覧と原則W1〜W6）

### 次にやる具体アクション
1. `requests/005-instruction-methods.md` の「検討すべき問い」5つを確認し、Phase 5（AIへの指示方法）を開始する
2. requests/005の開始時確認事項に従い、Phase 1・2の成果物（Skills・Workflow）を「どう呼び出す指示を書くか」という観点を設計に含める
3. 成果物ができ次第 `outputs/instruction-methods/` に保存し、`docs/current-state.md` と本ファイルを更新する
