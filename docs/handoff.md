# 引継ぎメモ（Handoff）

CLAUDE.mdの課題2「新しいチャットへ移る際、前チャットの内容を要約して引き継ぐと、情報が欠けたり表現が変わったりして意図しない方向へ進む」への対策。

**方針：自然文で要約しない。構造化された定型フォーマットで機械的に埋める。** 要約者（自分）の解釈が入る余地を減らすことが目的。

## 使い方

1. フェーズ（または長くなったセッション）を終える時、下の「最新の引継ぎ」ブロックを**今回の内容で上書き**する
2. 上書き前の内容は `archive/handoff/` に日付付きファイルとしてコピーしてから上書きする（履歴を残す）
3. 新しいチャットの最初にこのファイルの「最新の引継ぎ」だけを読めば再開できる状態を保つ

## テンプレート

```
### 完了したフェーズ
- Phase X: <フェーズ名>

### 確定した決定事項（変更しない前提）
- <決定1>（根拠：<理由>、記録先：<ファイルパス>）
- <決定2>

### 保留・要確認事項（次セッションで先に聞くこと）
- <question 1>

### 次セッションが最初に読むべきファイル
- <path1>
- <path2>

### 次にやる具体アクション
1. <action>
2. <action>
```

---

## 最新の引継ぎ

### 完了したフェーズ
- Phase 3: Context/RAG設計（旧引継ぎは `archive/handoff/2026-07-04-phase2.md`）

### 確定した決定事項（変更しない前提）
- 知識・状態・定義は**読み込みタイミングで5層に配置**する：L1常駐（AGENT.md）／L2状態／L3注入（`context/`）／L4参照（`knowledge/`）／L5定義。Phase 1・2持ち越しの「固有知識の恒久的な置き場」への回答は**L3注入層**（根拠：配置の議論を「いつ要るか」という判定可能な問いにする、記録先：`docs/decisions/0002-context-architecture.md`、`outputs/context-rag/01-context-architecture.md`）
- L1（AGENT.md）に残す基準は**3テスト（T1常時性・T2誤動作防止・T3地図性）＋分量予算150行目安**。不合格は削除ではなくL3/L4へ移してポインタ化（記録先：`outputs/context-rag/02-placement-criteria.md` 1〜2章）
- L3/L4の一次判定は「**従うもの（現在有効な判断基準）→L3／引くもの（事実・経緯・蓄積知見）→L4**」。両者は鮮度への義務が異なるため混在させない（記録先：同02の3章）
- **Vector DBは不採用**。RAGはファイルベースKB＋エージェント検索（L3はINDEXの名前解決・L4は分類→grep→中身）。再検討条件（名前で引けない検索の反復失敗／数千ファイル規模）と監視方法を明記して閉じた（記録先：ADR-0002、`outputs/context-rag/03-maintenance-cycle.md` 7章）
- コンテキスト圧縮は**構造化のみ（テンプレート充填・ファイル外部化・ポインタ化）、要約による圧縮は禁止**（記録先：同02の6章）
- Phase 2保留の「W3停止規約の実行時強制」は、**L1常駐の停止規約＋L3の知識マニフェスト `context/INDEX.md`（Skill前提入力→パス対応の唯一の置き場）＋「ファイルが存在し空でないか」への判定の機械化**として解消した（記録先：同01の4章）
- メンテナンスは**トリガー駆動**：新知識の既定の行き先はL4（AGENT.md直行禁止）／昇格・降格はコピーでなく移動／降格の第一歩はINDEXから／棚卸しはL1予算超過とG3到達で発火／矛盾は発見時に即時修正・削除（記録先：同03）

### 保留・要確認事項（次セッションで先に聞くこと）
- **このリポジトリのCLAUDE.mdへの適用は未実施**。02の5章が判定済み（追い出し3点：保存先表・委譲テンプレート・フェーズ完了定義。roadmapとの二重記載の解消を含む）。設計成果物の確定後の適用作業として、実施タイミングをユーザーに確認すること
- KB（L4）内部の分類体系・ディレクトリ構造・記録フォーマットの詳細設計は未着手 → **Phase 4（Knowledge Management設計）の中心**。03の「命名と分類の規律が検索インフラの代替」がPhase 4への要件になる
- 昇格①の回数集計・降格の「参照されない」の検出・棚卸し周期の最適化は計測の仕組みがない → Phase 7（運用設計）に委ねる
- Context体系（AGENT.md雛形・INDEX.md等）を実行可能な形で実装するかは未決定。現時点は設計ドキュメントのみ（ADR-0003の原則。実装するならADR-0006同様の部分改定ADRが要る）

### 次セッションが最初に読むべきファイル
- `docs/current-state.md`
- `requests/004-knowledge-management.md`
- `outputs/context-rag/01-context-architecture.md`（L4参照層の定義と書き込み経路。Phase 4の前提）
- `outputs/context-rag/03-maintenance-cycle.md`（特に2章書き込み規律・7章検索方式。Phase 4へ持ち越した要件の出どころ）

### 次にやる具体アクション
1. `requests/004-knowledge-management.md` の「検討すべき問い」を確認し、Knowledge Management設計を開始する
2. Phase 3が確定した前提（既定の行き先はL4・E3経由、検索はglob/grep、命名・分類の規律が検索インフラの代替）をKB内部構造の設計要件に組み込む
3. 成果物ができ次第 `outputs/knowledge-management/` に保存し、`docs/current-state.md` と本ファイルを更新する
