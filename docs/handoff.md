# 引継ぎメモ（Handoff）

CLAUDE.mdの課題2「新しいチャットへ移る際、前チャットの内容を要約して引き継ぐと、情報が欠けたり表現が変わったりして意図しない方向へ進む」への対策。

**方針：自然文で要約しない。構造化された定型フォーマットで機械的に埋める。** 要約者（自分）の解釈が入る余地を減らすことが目的。

## 使い方

1. フェーズ（または長くなったセッション）を終える時、下の「最新の引継ぎ」ブロックを**今回の内容で上書き**する
2. 上書き前の内容は `archive/handoff/` に日付付きファイルとしてコピーしてから上書きする（履歴を残す）
3. 新しいチャットの最初にこのファイルの「最新の引継ぎ」だけを読めば再開できる状態を保つ

## テンプレート

```
### 完了したフェーズ
- Phase X: <フェーズ名>

### 確定した決定事項（変更しない前提）
- <決定1>（根拠：<理由>、記録先：<ファイルパス>）
- <決定2>

### 保留・要確認事項（次セッションで先に聞くこと）
- <question 1>

### 次セッションが最初に読むべきファイル
- <path1>
- <path2>

### 次にやる具体アクション
1. <action>
2. <action>
```

---

## 最新の引継ぎ

### 完了したフェーズ
- Phase 6: 統合アーキテクチャ提案（G3承認 2026-07-06。旧引継ぎは `archive/handoff/2026-07-05-phase5.md`）

### 確定した決定事項（変更しない前提）
- 統合図は**「5層（いつ読むか）×実行主体（人／メインセッション／Subagent）」の配線図**。Phase 1〜5の決定だけで全体が1枚に配線でき、統合のための新要素はゼロ（根拠：全成果物がこの2軸平面に過不足なく載る、記録先：`outputs/architecture/01-integrated-architecture.md`）
- 7技術要素の採否：**採用＝Agent/Subagent・Memory（L2＋L4のファイルベースで実現済み）・RAG（概念のみ。実装はL4＋glob/grep検索）／不採用＝ベクトルDB（ADR-0002再確認）・Knowledge Graph・Workflow Engine・MCP（将来検討）**。全不採用に再検討条件（監視値・トリガーイベント）付き（根拠：「個人スケールでファイルシステムを超える基盤は要らない」＝導入コスト軸の一貫適用、記録先：`docs/decisions/0011-technology-selection.md`、`outputs/architecture/02-technology-decisions.md`）
- KGは**Phase 4 KB設計の代替にならない**（関係の索引は書き込み規律・分類・鮮度管理を代替しない）。補完としても関係情報の写し＝同期義務になるため現スケールでは過剰。再検討時も定型ヘッダの関係メタデータ拡充（軽量案）が先（記録先：同02の3.5）
- MCPは**「禁じないが依存しない」**：Skill/Workflowの【必須】入力がMCP経由でしか取得できない状態・L1〜L5の正がMCP先にしかない状態を作らない。この依存規定を破る変更はADR必須（記録先：同02の3.7、ADR-0011）
- Subagentは**「ゲート区間内の作業実行者」**。委譲禁止3点＝①ゲート裁定（人の専任）②L1/L3/L5への書き込み（人間の手続き限定）③F1/F2によるL2更新（メインセッション専任）。共通原理は「1系統であるべきものに主体を増やさない」。指示はメイン→Subagentも3要素定型＋ファイル参照（規定を分けない）。採用範囲は**ゲート区間内の単発委譲まで**（並列編成はPhase 7）（記録先：ADR-0011、`outputs/architecture/01-integrated-architecture.md` 4章）
- ツール組込みの**自動メモリ機能は体系の正とせず補助**：依存禁止・そこにしかない事実を作らない。昇格は原則2改定＝ADR必須（記録先：同02の3.2、ADR-0011）
- 整合性照合の結果は**照合20件・整合14・緊張6・矛盾0**。緊張T6（L1→Subagentの配線）は01への追補で解消済み（記録先：`outputs/architecture/03-consistency-check.md`）

### 保留・要確認事項（次セッションで先に聞くこと）
- **適用作業は残り3点**：①このリポジトリのCLAUDE.md軽量化 ③`research/` の移行（いずれも設計フェーズ完了後・ユーザー裁定2026-07-05）④棚卸しテンプレート等への「関係起因の検索失敗か」観点の追記（03の緊張T4）
- **Phase 7（運用設計）への引継ぎ事項**（03の5章）：T1 自動メモリの「補助」規定の遵守検出／T3 Subagentの途中経過保存の定型経路／T6 Subagent起動時にL1常駐規約が届いていることの実運用確認。加えてPhase 5以前からの持ち越し（形骸化検出・命名の質の劣化検出・観点ファイル肥大化整理基準など。`docs/current-state.md` 参照）
- **Phase 6成果物一式は未コミット**（00〜03・ADR-0011・状態ファイル更新・research/notes/ の再開メモが作業ツリーにある。コミットはユーザー判断）

### 次セッションが最初に読むべきファイル
- `docs/current-state.md`
- `requests/007-synthesis.md`（Phase 7の依頼定義）
- `outputs/architecture/01-integrated-architecture.md`・`02-technology-decisions.md`・`03-consistency-check.md`（Phase 6の主成果物。特に03の5章＝Phase 7への引継ぎ事項）

### 次にやる具体アクション
1. `requests/007-synthesis.md` を読み、Phase 7（統合・ロードマップ・ベストプラクティス）を開始する
2. Phase 7の設計計画に、03-consistency-check.md 5章の引継ぎ3件（T1・T3・T6）と運用設計の持ち越し事項（`docs/current-state.md` 参照）を組み込む
