# AGENT.md — 常駐規約（L1）

<!-- L1に書けるのは3テスト（T1常時性・T2誤動作防止・T3地図性）のいずれかに合格するものだけ。分量予算150行以内（wc -l で機械的に測る）。判定基準の正: outputs/context-rag/02-placement-criteria.md。記入が済んだらこのコメントは削除してよい -->

## プロジェクト地図

<!-- ここに書く: 主要な場所と役割の1画面一覧（T3地図性: 場所へのポインタのみ） -->
<!-- 書いてはいけない: 配置規約の詳細本文（→ context/ = L3へ）、構造探索で得た事実の記録（→ knowledge/ = L4へ） -->

| 場所 | 役割 |
|---|---|
| `context/` | 現在有効なプロジェクト固有の判断基準（従うもの・L3）。引き方は `context/INDEX.md` |
| `knowledge/` | 事実・経緯・蓄積知見（引くもの・L4）。記録フォーマットは `context/kb-format.md` |
| （記入: ソース・ドキュメント等の主要ディレクトリ） | （記入: 役割を1行で） |

## 常時適用の規約

<!-- ここに書く: どのタスクのセッションでも毎回効く行動規約だけ（T1常時性）、および誤ると全作業が壊れる前提の要点1行＋ポインタ（T2誤動作防止） -->
<!-- 書いてはいけない: 特定タスクでしか使わない規約・チェックリスト・テンプレート本文（→ context/ = L3へ）、作業の現在地・引継ぎ（→ 状態ファイル = L2へ）、経緯・調査結果（→ knowledge/ = L4へ） -->

- （記入: 例——回答の言語、毎応答に効く姿勢規約、全作業の前提となる決定の要点1行＋ADRへのポインタ）

## 停止規約（T2 — 削除しないこと）

- 作業・ステップの開始時、【必須】前提入力の充足を `context/INDEX.md` で確認する
- 対応する行が無い・行が指すパスにファイルが無い・空である場合は、**開始せず停止し**、「どの作業の・どの入力が・なぜ必要か」を明示してユーザーに要求する
- **推測による補完を禁止する**

## 知識の引き方・書き方（ポインタ）

- 固有規約（従うもの）は `context/INDEX.md` の対応表でパスを解決し、該当ファイルのみ読む。一括読み込みはしない
- 蓄積知見（引くもの）は `knowledge/` を分類→glob→grepの順で検索して引く
- 新しい知識の既定の行き先は `knowledge/`（フォーマットは `context/kb-format.md`）。本ファイルへの直接追記は禁止（昇格手順を経ること）
